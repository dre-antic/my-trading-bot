{
  "name": "AI Video Studio",
  "install": "./scripts/cloud-agent-install.sh",
  "terminals": [
    {
      "name": "studio-server",
      "command": "cd \"$(git rev-parse --show-toplevel)\" && source .venv/bin/activate && AIVS_HEADLESS=1 AIVS_PORT=8745 ./scripts/launch.sh --no-window",
      "description": "AI Video Studio local engine + web UI (http://127.0.0.1:8745). Runs headless (no native window)."
    }
  ],
  "ports": [
    { "name": "studio web UI", "port": 8745 }
  ]
}
